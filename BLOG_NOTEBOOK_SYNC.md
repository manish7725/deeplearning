# Blog → Notebook synchronization

The matching Markdown blog is the source of truth for lessons 01–37. The rebuild workflow reconstructs each notebook from its corresponding blog and validates the learning loop.

Final validation trigger.
